/**
 * Configuration for prod environment
 * this field would replace environment.ts if we were to do a production build
 * It's useless for the sujet.md, but it's for the example
 */
export const environment = {
  production: true,
  backendUrl: 'http://localhost:8080' // To be modified according to the production URL
};
